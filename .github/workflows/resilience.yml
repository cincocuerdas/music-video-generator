name: Redis Resilience

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'scripts/**'
      - 'prisma/**'
      - 'docker-compose.yml'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/resilience.yml'
  pull_request:
    paths:
      - 'src/**'
      - 'scripts/**'
      - 'prisma/**'
      - 'docker-compose.yml'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/resilience.yml'

jobs:
  redis-resilience:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Prepare env
        run: cp .env.example .env

      - name: Install dependencies
        run: npm ci

      - name: Run resilience test
        run: npm run test:resilience

      - name: Upload resilience logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: resilience-logs
          path: storage/tmp-tests/*
          if-no-files-found: ignore

      - name: Cleanup containers
        if: always()
        run: docker compose down -v

  throttling-abuse:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Prepare env
        run: cp .env.example .env

      - name: Install dependencies
        run: npm ci

      - name: Run throttling abuse test
        run: npm run test:throttling

      - name: Upload throttling logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: throttling-logs
          path: storage/tmp-tests/*
          if-no-files-found: ignore

      - name: Cleanup containers
        if: always()
        run: docker compose down -v
